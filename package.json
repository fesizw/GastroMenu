{
  "name": "gastromenu-parallel",
  "private": true,
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm turbo run dev --parallel",
    "build": "pnpm turbo run build --parallel",
    "start": "pnpm turbo run start --parallel",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "apps/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.7.1",
  "dependencies": {
    "turbo": "^2.5.0"
  }
}